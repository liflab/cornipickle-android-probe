# Define what it means the flat button
#link https://material.io/guidelines/components/buttons.html#buttons-style
We say that $x is Button1 when (
  ((($x's width / the device's device-density)  is greater than  47 ) And (($x's height / the device's device-density) is greater than  47))   And
  ( Not ($x's bgcolor matches "RGB\(0,0,0\)") )

).


"""
  @name Menus aligned
  @description All list items
  @severity Warning
"""

For each $z in $(Button) (

$z is Button1
).