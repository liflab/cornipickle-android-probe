
"""
@description The value in the "#textView4" element always increases
@name Increasing value
@severity Error
"""
Always (
  For each $x in $(#textView4) (
Next (
      For each $y in $(#textView4) (
      0 equals 1
      )
      )
    )

).

